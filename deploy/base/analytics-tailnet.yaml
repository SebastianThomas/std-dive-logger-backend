apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: analytics-tailnet-state
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: local-path
  resources:
    requests:
      storage: 128Mi
