apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: analytics-tailnet-tls
spec:
  secretName: analytics-tailnet-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - '*.ts.homelab.sthomas.ch'
