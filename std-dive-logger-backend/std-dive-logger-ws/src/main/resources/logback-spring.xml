<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty scope="context" name="springAppName" source="spring.application.name"/>
    <property name="LOG_FILE" value="${springAppName}"/>

    <!-- You can override this to have a custom pattern -->
    <!-- Make sure not to remove LOG_LEVEL_PATTERN, as it's needed by Sleuth -->
    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %clr(%marker){green} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <!-- Appender to log to console -->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <logger name="ch.sthomas.stddivelogger" level="DEBUG"/>
    <logger name="org.springframework.web" level="DEBUG"/>
    <logger name="org.springframework.security" level="DEBUG"/>
    <!-- Malformed/non-HTTP bytes hitting the port (mass internet scanning/probing, not a real
         request - Tomcat already rejects the connection outright) log an INFO-level stack trace
         per occurrence otherwise; this is background noise on any exposed port, not something
         actionable, so it's quieted below the level it's actually logged at. -->
    <logger name="org.apache.coyote.http11.Http11Processor" level="ERROR"/>

    <!-- Log SQL statements -->
    <!--    <logger name="org.hibernate.SQL" level="DEBUG"/>-->
    <!-- Log JDBC parameters (optional, shows bound values) -->
    <!--    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE"/>-->

    <root level="INFO">
        <appender-ref ref="console"/>
    </root>
</configuration>
